## 1. Efficiency contract, limits, and policy resolution

- [x] 1.1 Define immutable context-efficiency domain types for bounded metrics, evidence/report identity, calibration summary identity, freshness, diagnostic categories, and hard-failure categories; verify `go test ./internal/retrieval ./internal/memory -count=1` covers rate/count bounds, stable identity, redaction allowlists, and invalid-value rejection.
- [x] 1.2 Add deployment configuration parsing and validation for context-calibration hard limits—summary age, minimum evidence, decay window, contribution cap, candidate/context work, and elapsed time—and verify `go test ./internal/config -count=1` proves safe defaults and invalid/zero/over-limit values fail closed.
- [x] 1.3 Extend the versioned exact-scope ranking rollout payload with optional calibration policy/version fields, preserving old payload behavior; verify `go test ./internal/memory ./internal/storage/postgres -count=1` covers JSON round-trip, malformed-field rejection, expiry, exact-scope selection, and baseline behavior with no calibration policy.
- [x] 1.4 Resolve effective calibration limits only for authorized context/search rollout surfaces, allowing policy values only to narrow deployment caps; verify `go test ./internal/retrieval -count=1` covers deterministic policy identity, absent/expired/malformed fallback, diagnostics/shadow/active states, and unsupported-surface rejection.

## 2. Derived calibration summaries

- [x] 2.1 Add additive, reversible PostgreSQL migration(s) for exact-scope calibration summaries, source watermarks, policy/version identity, bounded aggregate fields, and lookup indexes; verify migration upgrade/rollback and manifest tests pass without rewriting canonical memories, raw events, provenance, feedback, task evaluations, or existing ranking policies.
- [x] 2.2 Add scoped repository read/write interfaces for calibration summaries that require resolved scope, policy identity, source watermark, and bounded limits; verify `go test ./internal/storage/postgres -count=1` rejects missing scope/identity, stale/invalid summaries, and unbounded list inputs.
- [x] 2.3 Implement deterministic summary rebuild from active non-superseded usefulness feedback, task evaluations, verification, and approved quality summaries; verify fixed-clock tests prove exact-scope isolation, source watermark calculation, stable ordering, and no raw feedback text or high-cardinality identities in the summary.
- [x] 2.4 Apply configured minimum evidence, confidence threshold, decay, and per-signal contribution cap during summary rebuild; verify tests exclude below-threshold, expired, superseded, foreign, lifecycle-hidden, and malformed source evidence while retaining durable source history.
- [x] 2.5 Wire summary rebuild/repair through the existing bounded worker and lease model with idempotent run identity, stale recovery, and redacted telemetry; verify `go test ./internal/jobs ./internal/app ./internal/telemetry -count=1` covers duplicate dispatch, lease recovery, bounded batches, unavailable sources, and no canonical mutation.

## 3. Context packing and governed rollout

- [x] 3.1 Add an optional calibration-summary reader to context/search assembly dependencies and pass only already eligible, deduplicated, diversity-selected candidates into it; verify `go test ./internal/retrieval -count=1` proves calibration cannot introduce candidates, alter scope/lifecycle/temporal eligibility, rewrite citations, or expand graph traversal.
- [x] 3.2 Implement deterministic weak-priority adjustment using compatible fresh summaries, cap/clamp rules, protected evidence groups, and existing baseline tie-breakers; verify fixed-clock tests cover identical replay, insufficient-summary baseline fallback, equal-score stable ordering, and protected-evidence retention under a tight budget.
- [x] 3.3 Enforce caller/service context budgets after calibration and retain existing section names, citation rendering, and omission accounting; verify context assembly tests prove no token/character overflow, no new section, and bounded baseline fallback when a calibrated item cannot fit.
- [x] 3.4 Wire diagnostics-only and shadow policies to compute redacted calibration/efficiency evidence while returning byte-equivalent ordinary baseline responses; verify integration tests compare ordinary search/context outputs and demonstrate that policy disable/rollback restores baseline without data mutation.
- [x] 3.5 Enable active-for-exact-scope calibration only after a compatible fresh summary and valid policy are present; verify service tests cover no-policy baseline, stale/missing summary fallback, scope mismatch rejection, request-local explicit behavior, and immediate rollback.

## 4. Evaluation, release gate, telemetry, and API protection

- [x] 4.1 Extend authorized evaluation fixtures, replay models, and reports with bounded relevant-token ratio, evidence density, duplicate/stale rates, quality-per-budget, per-pass cost, budget outcomes, and calibration identity; verify `go test ./internal/retrieval -count=1` rejects incompatible identities and redacts query/content/IDs/scope/DSN/feedback/raw-score values.
- [x] 4.2 Add fixed-clock baseline-versus-calibrated fixtures spanning summary/projection/canonical/chunk evidence, 0/1/2/3-hop plans, temporal current/as-of/during queries, duplicate/stale candidates, tight budgets, sparse/noisy/superseded feedback, and protected evidence groups; verify retrieval and PostgreSQL suites pass repeatedly with `-count=1`.
- [x] 4.3 Encode hard release failures for protected recall decline, cross-scope or lifecycle leakage, citation mismatch, stale/duplicate rate overflow, budget overflow, stale summary, unbounded work, nondeterministic replay, public API leakage, and rollback failure; verify every fixture remains non-pass despite positive quality-per-budget gain.
- [x] 4.4 Add allowlisted telemetry and authorized diagnostic labels for rollout stage, summary freshness, metric bucket, omission category, feedback evidence category, policy version, and aggregate count; verify telemetry tests reject arbitrary/sensitive labels and ordinary APIs omit calibration internals.
- [x] 4.5 Preserve OpenAPI and MCP ordinary search/context response contracts while active calibration is enabled; verify HTTP, MCP, and OpenAPI tests prove feedback history, calibration weights, raw metrics, candidate pools, policy fields, hidden IDs, and trajectory details are absent.

## 5. Operational evidence and documentation

- [x] 5.1 Update self-hosting, retrieval release-gate, configuration, and governance documentation for disabled-by-default calibration, exact-scope policy narrowing, summary freshness/rebuild, redacted diagnostics, hard gates, rollback, and owned PostgreSQL evaluation; verify documented configuration names and Markdown links match implementation.
- [x] 5.2 Reconcile the roadmap status after archived RQ2/RQ3 and describe RQ4 as the current retrieval-quality proposal without claiming active rollout authorization; verify the roadmap/OpenSpec archive/status consistency check passes.
- [x] 5.3 Run an exact-scope owned PostgreSQL + pgvector shadow evaluation against the RQ1/RQ2/RQ3 baseline and capture redacted quality, efficiency, latency, safety, citation, replay, summary-freshness, and rollback evidence; verify a missing DSN or any hard gate remains a controlled non-pass.
- [x] 5.4 Run the required repository verification suite—targeted packages, `go test ./... -count=1 -timeout 15m`, `go test -race ./... -timeout 20m`, `go vet ./...`, `openspec validate --all --strict`, and `git diff --check`—and record outcomes before requesting integration review.
