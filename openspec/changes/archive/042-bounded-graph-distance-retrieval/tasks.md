## 1. Traversal contract, limits, and policy resolution

- [x] 1.1 Define immutable graph traversal domain types—deployment limits, effective limits, policy/version identity, path proof, bounded result, and allowed diagnostic categories—and verify `go test ./internal/retrieval ./internal/memory -count=1` covers defaults, normalization, stable identities, redaction allowlists, and invalid-value rejection.
- [x] 1.2 Add deployment configuration parsing and validation for `graph_max_hops` (default `3`) and seed/edge/path/candidate/time ceilings; verify `go test ./runtime/config -count=1` proves omitted configuration uses the default and invalid/zero/over-limit values fail closed.
- [x] 1.3 Extend the versioned exact-scope retrieval rollout policy payload with optional graph traversal policy fields, preserving backward compatibility for existing payloads; verify `go test ./internal/memory ./internal/storage/postgres -count=1` covers JSON round-trip, unknown/malformed field rejection, expiry, exact-scope selection, and no-policy baseline behavior.
- [x] 1.4 Resolve effective graph limits only for `entity_relation` and `multi_hop` plans, requiring every scope policy value to be less than or equal to its deployment hard cap; verify `go test ./internal/retrieval -count=1` covers default one hop, policy-selected 0/1/2/3 hops, cap rejection, unsupported-family rejection, and deterministic plan identity.

## 2. PostgreSQL relation-path expansion

- [x] 2.1 Specify and add additive, reversible PostgreSQL migration(s) for the exact relation-projection traversal predicate and join/index access pattern; verify the migration upgrade/rollback tests pass and no migration rewrites canonical memories, fact versions, provenance, audit history, or relation projections.
- [x] 2.2 Add a repository graph-expansion interface and request-scoped input/output models separate from one-hop `SearchRelations`; verify `go test ./internal/storage/postgres -count=1` confirms callers cannot supply unbounded depth or omit resolved scope/time/source constraints.
- [x] 2.3 Implement a parameterized recursive CTE over `relation_projections` that applies exact scope, authorization, lifecycle, valid-time, and RQ2 source-currency predicates to seeds and every recursive edge; verify PostgreSQL integration tests exclude foreign, hidden, suppressed, forgotten, deleted, expired, future-invalid, and source-version-stale evidence at all hops.
- [x] 2.4 Implement path-local repeated-node and repeated-edge detection in the CTE/result mapping and verify PostgreSQL integration tests stop cycles without duplicate or infinite path results.
- [x] 2.5 Apply per-hop, per-seed, and request-wide edge/path/candidate/time limits in the traversal query/result boundary; verify PostgreSQL integration tests prove each exhaustion category stops only further graph work and retains already eligible bounded results.
- [x] 2.6 Apply deterministic path ordering by hop length, relation confidence, source reliability, temporal/projection freshness, and stable identity; verify fixed-clock replay tests return identical proofs and endpoint order across repeated executions.

## 3. Retrieval-plan and candidate integration

- [x] 3.1 Add graph traversal disposition and effective policy identity to immutable retrieval plans without enabling it for unsupported families; verify `go test ./internal/retrieval -count=1` covers replay stability, malformed policy fallback, and unchanged baseline plans for non-graph families.
- [x] 3.2 Integrate graph expansion after approved relation baseline seeds are available, passing the original request context and all resolved constraints; verify service tests prove no expansion occurs without a valid active exact-scope policy and default enabled traversal performs one hop.
- [x] 3.3 Convert qualified traversal endpoints into in-memory-only path proofs and canonical candidates with source-version citations, then submit them as stable relation-channel ranks to existing fusion/deduplication; verify `go test ./internal/retrieval -count=1` covers canonical identity deduplication, citation/provenance alignment, and absence of raw-score addition.
- [x] 3.4 Implement baseline-safe failure closure for projection absence, database error, context cancellation, authorization failure, policy rejection, proof failure, and budget exhaustion; verify service tests return the RQ1/RQ2 baseline and never expose partial or unqualified graph state.
- [x] 3.5 Wire diagnostics-only, shadow, and active-for-exact-scope graph behavior into the governed rollout flow; verify integration tests show ordinary diagnostics/shadow responses equal baseline and policy disable/rollback immediately restores baseline without data mutation.

## 4. Diagnostics, API protection, and operational evidence

- [x] 4.1 Add authorized graph traversal diagnostics and allowlisted telemetry labels for family, rollout stage, hop bucket, relation category, policy version, exclusion/truncation, budget, and aggregate count; verify telemetry/unit tests reject arbitrary labels and sensitive values.
- [x] 4.2 Preserve ordinary HTTP and MCP search/context response models while graph traversal is active; verify contract tests prove that raw path text, node/edge/memory IDs, hop counts, distances, raw scores, policy fields, diagnostics, scope values, and hidden topology are absent.
- [x] 4.3 Extend authorized evaluation fixtures and reports with redacted graph aggregate metrics—candidate count by hop, budget/truncation/fallback rates, citation coverage, eligible relation-hit rate, latency bucket, and quality delta; verify report serialization contains no query text, content, IDs, scope, DSN, credentials, provider payload, or raw score.
- [x] 4.4 Encode graph hard release failures for cross-scope traversal, source/lifecycle/temporal ineligibility, untruncated cycles, limit overflow, citation/provenance mismatch, ordinary API leakage, and nondeterministic replay; verify any such fixture produces a non-pass verdict despite a positive quality delta.

## 5. End-to-end verification and documentation

- [x] 5.1 Add deterministic fixed-clock fixtures covering 0/1/2/3 hops, default one hop, source corrections, missing/stale projections, multi-hop evidence, high-degree seeds, duplicate paths, and every scope/lifecycle/temporal boundary; verify targeted retrieval and PostgreSQL integration suites pass repeatedly with `-count=1`.
- [x] 5.2 Run an exact-scope PostgreSQL shadow evaluation against the RQ1/RQ2 baseline and capture redacted index/latency, quality, safety, citation, deterministic replay, and rollback evidence; verify the release report remains non-pass if any prerequisite or hard gate is missing. Controlled non-pass recorded on 2026-09-22 because the independently owned evaluation DSN prerequisite was absent; no active rollout was authorized.
- [x] 5.3 Update runtime/configuration, rollout, diagnostic, and retrieval-governance documentation to describe default one-hop behavior, maximum three-hop hard cap, policy narrowing, redaction, and rollback; verify Markdown links and documented configuration names match implementation.
- [x] 5.4 Run the required repository verification suite—targeted packages, `go test ./... -count=1 -timeout 15m`, `go test -race ./... -timeout 20m`, `go vet ./...`, `openspec validate --all --strict`, and `git diff --check`—and record outcomes before requesting integration review. All commands passed on 2026-09-22; the owned PostgreSQL shadow evaluation remains a controlled non-pass until its independent DSN prerequisite is available.
